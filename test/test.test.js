const assert = require('assert');

describe('A Feature', () => {
  describe('A Scenarion', () => {
    it('Does someting expected', () => {
      assert(true);
    });
  });
});

describe('dasdas', () => {
  describe('A Scenarion', () => {
    it('Does someting expected', () => {
      assert(true);
    });
  });
});
